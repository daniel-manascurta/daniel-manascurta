name: second

on:
  workflow_call:
    inputs:
      pass_vars:
        required: true
        type: string
jobs:
  runtest:
    runs-on: ubuntu-latest
    steps:

    - name: Run a multi-line script
      run: echo ${{ inputs.pass_vars }}
