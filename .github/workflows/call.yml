name: callable

on:
  workflow_call:
    inputs:
      pass_vars:
        required: true
        type: string
jobs:
  runtest:
    runs-on: ubuntu-24.04
    steps:
    - name: Run a multi-line script
      run: echo ${{ inputs.pass_vars }}
